import mongoose from "mongoose";

let isConnected = false; // global variable to cache the connection

export const connectToDB = async () => {
    mongoose.set('strictQuery', true);
    is (isConnected) {

    }
}